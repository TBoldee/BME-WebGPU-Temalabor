export class Player {
    x: number;
    y: number;
    w: number;
    h: number;

    constructor(x: number, y: number) {
        this.x = x;
        this.y = y;
        this.w = 20;
        this.h = 60;
    }
}